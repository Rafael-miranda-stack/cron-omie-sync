name: Despertador TPL Omie

on:
  schedule:
    - cron: '3,13,23,33,43,53 * * * *'
  workflow_dispatch:

jobs:
  disparar:
    runs-on: ubuntu-latest
    steps:
      - name: Acionar Render
        run: |
          curl --fail --silent --show-error -X POST "https://tpl-omie-sync-render-3pvj.onrender.com/sincronizar" \
               -H "Authorization: Bearer ${{ secrets.OMIE_TOKEN }}" \
               -H "X-Sync-Source: github-actions"
