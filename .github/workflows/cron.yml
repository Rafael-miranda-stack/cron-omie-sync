name: Despertador TPL Omie

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  disparar:
    runs-on: ubuntu-latest
    steps:
      - name: Acionar Render
        run: |
          curl -s -X POST "https://tpl-omie-sync-render-3pvj.onrender.com/sincronizar" \
               -H "Authorization: Bearer ${{ secrets.OMIE_TOKEN }}"
